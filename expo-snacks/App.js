/**
 * React Native Leader Line - Combined Demo
 * 
 * This Expo Snack contains all the examples and demos for react-native-leader-line
 * in a single app with tab navigation.
 */

import { AppRegistry } from 'react-native';
import CombinedDemo from './components/CombinedDemo';

AppRegistry.registerComponent('main', () => CombinedDemo);

export default CombinedDemo;